# AICU 呼吸道菌群预后研究 — 分析开发指南

> 本文档面向**服务器端**的后续分析开发与维护。  
> 代码仓库为 `Hospital_Code/`，研究方案原文见上级目录 `ALL/AICU呼吸道菌群预后研究——完整研究方案与预期结果.docx`。  
> **本地仓库不含完整原始数据**；临床表与 16S 测序结果均在服务器上，部署后需通过 `config.yaml` 指向真实路径。

---

## 1. 文档目的

| 用途 | 说明 |
|------|------|
| 部署 | 将本仓库 push 到服务器后，如何配置环境、数据路径并跑通全流程 |
| 开发 | 各分析模块职责、扩展方式、与研究方案的对应关系 |
| 协作 | 统一变量命名、输出规范、版本记录方式 |
| 验收 | 正式分析前/后的检查清单 |

---

## 2. 研究方案与代码映射

研究采用**三层分析架构**，与代码模块一一对应：

```
研究方案                          代码模块                              输出
─────────────────────────────────────────────────────────────────────────────────
第一层：描述性分析
  基线特征表                        src/analysis/baseline.py              Table 1
  菌群全景（门/属/稀释曲线）         src/analysis/microbiome_desc.py       Figure 1
  α 多样性组间比较                  src/analysis/alpha_beta.py            Figure 2
  β 多样性 PCoA + PERMANOVA         src/analysis/alpha_beta.py            Figure 3
  LEfSe 差异菌群 + 热图             src/analysis/differential.py          Figure 4

第二层：关联分析（核心）
  菌群-炎症 Spearman 相关           src/analysis/inflammation_axis.py     Figure 5
  四象限联合分层                    src/analysis/inflammation_axis.py     Figure 6
  中介分析（Bootstrap）             src/analysis/mediation.py             Figure 7

第三层：预测模型
  三组模型 LOO-CV + ΔAUC            src/analysis/prediction.py            Figure 8, Table 2
  多方法共识关键菌群                src/analysis/biomarkers.py            Figure 9
```

主入口：`run_analysis.py`（按顺序调用上述 9 步）。

---

## 3. 服务器部署流程

### 3.1 推荐目录结构（服务器）

建议在服务器上保持「代码 + 数据」分离，例如：

```
/path/to/project/
├── Hospital_Code/              ← 本仓库（git clone / pull）
│   ├── config.yaml
│   ├── config.server.yaml      ← 建议：服务器专用配置（不提交敏感路径时可本地维护）
│   ├── run_analysis.py
│   └── ...
└── data/                       ← 服务器原始数据（不入 git）
    ├── clinical/
    │   └── 临床样本收集表3.xlsx
    └── microbiome/
        ├── asv_table.csv       ← DADA2 ASV 丰度表
        ├── taxonomy.csv        ← SILVA 注释表
        └── （可选）sample_metadata.csv
```

### 3.2 首次部署命令

```bash
# 1. 拉取代码
cd /path/to/project/Hospital_Code
git pull

# 2. 创建虚拟环境
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# 3. 修改配置（见下一节）
vim config.yaml   # 或 cp config.server.yaml config.yaml

# 4. 运行全流程
export MPLCONFIGDIR=$(pwd)/.mplconfig   # 避免 matplotlib 缓存权限问题
python run_analysis.py

# 5. 指定配置文件（可选）
python run_analysis.py --config config.server.yaml
```

### 3.3 服务器 `config.yaml` 必改项

部署到服务器后，**至少**修改以下内容：

```yaml
paths:
  # 改为服务器上临床 Excel 的绝对或相对路径
  clinical_data: "/path/to/data/clinical/临床样本收集表3.xlsx"
  microbiome_asv: "/path/to/data/microbiome/asv_table.csv"
  taxonomy: "/path/to/data/microbiome/taxonomy.csv"

analysis:
  use_demo_microbiome: false   # 服务器有真实数据时必须设为 false
```

### 3.4 本地 vs 服务器差异

| 项目 | 本地开发 | 服务器正式分析 |
|------|----------|----------------|
| 临床数据 | `../ALL/临床样本收集表3.xlsx` | 服务器 data 目录 |
| 16S ASV | 无 → 演示数据 | 真实 DADA2 输出 |
| `use_demo_microbiome` | `true`（联调用） | **`false`** |
| 输出 | `output/` | 同左，建议按日期归档 |

运行成功后检查 `output/run_summary.json`：

```json
{
  "n_samples": 42,
  "demo_microbiome": false   ← 正式分析必须为 false
}
```

---

## 4. 数据规范

### 4.1 临床数据（Excel）

- **文件**：优先使用 `临床样本收集表3.xlsx`（含 ASA 分级，字段最全）
- **Sheet**：`Sheet1`，第 1 行为大标题、第 2 行为列名、第 3 行起为数据
- **样本主键**：`样本编号`（如 `A1`, `A2`…），与 ASV 表行索引一致

**加载后标准化字段**（`src/data_loader.py`）：

| 内部字段名 | Excel 来源 | 说明 |
|------------|------------|------|
| `sample_id` | 样本编号 | 索引 |
| `sex` | 性别 | 男=1，女=0 |
| `age` | 年龄 | 自动解析「69岁」 |
| `bmi` | BMI | |
| `smoking` | 个人史 | 含吸烟→1 |
| `hypertension` / `diabetes` / `copd` | 病史 | 关键词匹配 |
| `asa` | ASA分级 | I–V → 1–5 |
| `surgery_duration_min` | 手术时长（min） | |
| `anesthesia_duration_min` | 麻醉时长（min） | |
| `opioid_morphine_mg` | 阿片类药物列 | 多列时取最后有效数值 |
| `extubation_time_min` | 拔管时间（min） | 预后连续变量 |
| `icu_stay_min` | AICU滞留时间(min) | |
| `adverse_event` | 术后不良反应 | 「无」→0，否则→1 |
| `qor15` | 术后第1天QoR-15量表评分 | |
| `wbc` / `crp` / `pct` / `nlr` | 对应检验列 | 自动去单位 |

带单位的字符串（如 `11.35*10^9/L`）会在加载时自动提取数值。

### 4.2 菌群数据（16S）

#### ASV 丰度表 `asv_table.csv`

```
sample_id,ASV_001,ASV_002,...
A1,1523,89,...
A2,2104,45,...
```

- **行**：样本编号，必须与临床表 `样本编号` 一致
- **列**：ASV ID
- **值**：原始 reads count（非相对丰度）
- **来源**：DADA2 流程，V3–V4，测序深度建议 ≥ 30,000 reads/样本

#### 分类注释表 `taxonomy.csv`

```
,Genus,Family,Phylum,Species
ASV_001,Streptococcus,Streptococcaceae,Firmicutes,Streptococcus mitis
ASV_002,Pseudomonas,Pseudomonadaceae,Proteobacteria,Pseudomonas aeruginosa
```

- **行索引**：ASV ID（与丰度表列名一致）
- **必需列**：`Genus`、`Phylum`（Figure 1 门/属水平聚合依赖此二列）

### 4.3 样本对齐规则

`src/preprocessing.py` → `merge_microbiome()`：

1. 取临床表与 ASV 表 **样本 ID 交集**
2. 若无交集 → 报错，需检查编号格式（大小写、空格）
3. 合并后写入 `clinical.attrs["asv"]`、`rel_abund`、`taxonomy`、`bray_curtis`

**开发建议**：在服务器首次跑通前，执行样本对齐检查：

```python
import pandas as pd
clinical_ids = set(pd.read_excel("临床表路径")["样本编号"].astype(str))
asv_ids = set(pd.read_csv("asv_table.csv", index_col=0).index.astype(str))
print("仅临床有:", clinical_ids - asv_ids)
print("仅 ASV 有:", asv_ids - clinical_ids)
print("交集:", len(clinical_ids & asv_ids))
```

---

## 5. 纳入排除标准（待完善）

研究方案中的排除标准，**当前代码尚未自动执行**，需在服务器开发阶段补充到 `src/preprocessing.py`：

| 排除条件 | 建议实现方式 | 状态 |
|----------|--------------|------|
| 术前明确呼吸道感染 | 临床表字段 / 病史关键词 | ⬜ 待开发 |
| 近 4 周抗生素或免疫抑制剂 | 新增列或病史解析 | ⬜ 待开发 |
| 严重免疫缺陷 | 病史字段 | ⬜ 待开发 |
| 术前气管切开 / 长期机械通气 | 病史 / 手术史 | ⬜ 待开发 |
| 测序质量不合格 | ASV 表 + QC 指标文件 | ⬜ 待开发 |
| 临床资料缺失 > 20% | 按字段缺失率计算 | ⬜ 待开发 |

**开发模板**（建议在 `preprocessing.py` 新增函数）：

```python
def apply_inclusion_exclusion(clinical: pd.DataFrame, cfg: dict) -> pd.DataFrame:
    """按研究方案过滤样本，并记录排除日志到 output/exclusion_log.csv"""
    ...
    return clinical_filtered
```

在 `run_analysis.py` 中于 `add_outcome_groups()` 之后调用。

---

## 6. 分组与结局变量

配置项见 `config.yaml` → `grouping`：

| 变量 | 定义 | 代码字段 |
|------|------|----------|
| 拔管时间（连续） | 入 AICU 至成功拔管（min） | `extubation_time_min` |
| 早期/延迟拔管（二分类） | 以中位数或固定阈值划分 | `delayed_extubation`, `extubation_group` |
| ICU 滞留时长 | 入 AICU 至出室（min） | `icu_stay_min` |
| 术后不良反应 | 低氧血症/喉痉挛/肺不张等任一 | `adverse_event` |
| QoR-15 | 术后第 1 天评分 | `qor15` |
| 四象限分组 | Shannon 中位数 × CRP 10 mg/L | `quadrant_group` |

默认：`extubation_split: median`。若需与方案示例一致（68.5 min），改为 `fixed` 并设置 `extubation_fixed_min: 68.5`。

---

## 7. 模块开发说明

### 7.1 新增分析步骤

1. 在 `src/analysis/` 新建模块，导出 `run_xxx(clinical, output_dir, **cfg)` 函数
2. 在 `run_analysis.py` 中 import 并调用
3. 图表统一用 `src/visualization/style.py` 的 `apply_style()` + `save_figure()`
4. 统计检验复用 `src/utils/stats.py`

### 7.2 关键扩展点（按优先级）

#### P0 — 服务器首次正式跑通

- [ ] 配置真实数据路径，`use_demo_microbiome: false`
- [ ] 确认样本交集数量与预期一致（方案示例 n=42，当前临床表约 59 例，需纳入排除后确定）
- [ ] 核对 Table 1 各列 P 值与分组人数

#### P1 — 与研究方案完全对齐

- [ ] 实现纳入排除逻辑（第 5 节）
- [ ] LEfSe：当前为 Python 近似实现（Mann-Whitney + LDA），可考虑对接真实 LEfSe 或 ANCOM-BC2
- [ ] 预测模型：方案提到「轻量级深度学习概念验证」→ 可在 `prediction.py` 增加 MLP + LOO-CV 分支
- [ ] 多因素 Logistic（Figure 9 描述）：控制 ASA、麻醉时长后的 OR → 新增 `src/analysis/multivariable.py`

#### P2 — 论文出图优化

- [ ] 图注改为英文/中文双语（投稿期刊要求）
- [ ] 导出 PDF/SVG 矢量图（`visualization.fig_format`）
- [ ] 统一期刊配色与字体（Nature/Lancet 模板）
- [ ] Results 段落自动生成（基于 `output/` 统计量模板）

### 7.3 修改现有模块时的注意点

| 模块 | 注意 |
|------|------|
| `differential.py` | LEfSe LDA 阈值由 `lda_threshold` 控制，样本量小易无显著属 |
| `mediation.py` | 路径：Shannon → log(CRP) → 拔管时间；样本 < 10 会跳过 |
| `prediction.py` | LOO-CV 在小样本下 AUC 波动大；特征含菌群属丰度 + PCoA |
| `biomarkers.py` | 共识菌属需 ≥2 种方法同时选中，否则取 Top 4 |

---

## 8. 输出物规范

### 8.1 目录

```
output/
├── run_summary.json              # 运行摘要（样本量、是否演示数据）
├── processed_clinical_data.csv   # 清洗后合并临床+多样性字段
├── tables/
│   ├── table1_baseline.csv/xlsx
│   └── ...
└── figures/
    ├── figure1_microbiome_overview.png
    ├── ...
    ├── table2_model_performance.csv/xlsx
    └── figure9_key_biomarkers.png
```

### 8.2 正式分析归档建议

每次正式运行按日期备份，避免覆盖：

```bash
RUN_ID=$(date +%Y%m%d_%H%M)
cp -r output "output_archive/${RUN_ID}"
```

在 `run_summary.json` 中已含 `run_time`，建议后续增加 `git_commit` 与 `config_hash` 字段便于追溯。

---

## 9. 开发工作流

### 9.1 日常流程

```bash
git pull
source .venv/bin/activate
# 修改 src/ 或 config
python run_analysis.py --config config.server.yaml
# 检查 output/
git add src/ config.yaml 分析开发指南.md
git commit -m "feat: 描述改动"
git push
```

### 9.2 分支建议

| 分支 | 用途 |
|------|------|
| `main` | 服务器可运行的稳定版 |
| `dev/analysis` | 新分析模块开发 |
| `dev/figures` | 出图样式调整 |

### 9.3 不建议提交到 git 的内容

```
.venv/
.mplconfig/
output/              # 运行产物（可选 .gitkeep）
output_archive/
data/microbiome/*.csv   # 真实测序数据
config.server.yaml      # 若含服务器绝对路径（可用 config.server.example.yaml 代替）
```

建议在仓库根目录添加 `.gitignore`（若尚未添加）。

---

## 10. 常见问题排查

| 现象 | 可能原因 | 处理 |
|------|----------|------|
| `临床样本与 ASV 表无交集` | 样本编号不一致 | 统一为 `A1` 格式，去空格 |
| `demo_microbiome: true` | ASV 路径错误或文件不存在 | 检查 `paths.microbiome_*` |
| Table 1 P 值全 NA | 某组样本量为 0 | 检查拔管时间缺失（「无」） |
| Figure 4 无差异属 | 样本量小 / LDA 阈值过高 | 降低 `lda_threshold` 或检查真实数据 |
| matplotlib 字体/cache 报错 | 服务器无写权限 | `export MPLCONFIGDIR=$(pwd)/.mplconfig` |
| QoR-15 大量缺失 | 临床表未填 | 方案 n=42 可能为填完 QoR 后的子集 |
| 阿片类药物解析异常 | Excel 多列同名 | 已在 `data_loader.py` 处理，新列需更新解析逻辑 |

---

## 11. 依赖与环境

- **Python**：≥ 3.10（已在 3.14 测试通过）
- **依赖清单**：`requirements.txt`
- **核心库**：pandas, scipy, scikit-learn, statsmodels, matplotlib, seaborn, shap

服务器若无图形界面，`matplotlib` 默认 `Agg` 后端即可保存 PNG，无需 DISPLAY。

---

## 12. 版本记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-07-09 | v0.1.0 | 初始流水线：Table 1 + Figure 1–9 + Table 2；本地演示菌群联调 |
| | | 待服务器：接入真实 ASV、纳入排除、正式 n 确定 |

---

## 13. 相关文件索引

| 文件 | 说明 |
|------|------|
| `README.md` | 快速上手（简明版） |
| `分析开发指南.md` | 本文档（服务器开发详细版） |
| `config.yaml` | 分析参数与路径 |
| `run_analysis.py` | 主入口 |
| `ALL/AICU呼吸道菌群预后研究——完整研究方案与预期结果.docx` | 研究方案与预期结果（图表描述参考） |

---

## 14. 下一步行动清单（服务器端）

1. **Clone / Pull** 本仓库到服务器分析目录  
2. **确认数据路径**，复制或软链 ASV、taxonomy、临床 Excel  
3. **修改 `config.yaml`**：`use_demo_microbiome: false`  
4. **运行** `python run_analysis.py`，确认 `demo_microbiome: false`  
5. **核对** Table 1 样本量与纳入排除后 n 是否符合方案  
6. **逐项对比** Figure 1–9 与方案 docx 中的图表结构  
7. **开发 P1 项**：纳入排除、多因素回归、深度学习验证分支  
8. **归档** 第一次正式运行的 `output/` 作为论文初稿底稿  

如有服务器实际数据目录结构，可在本文档第 3.1 节补充具体路径，便于团队统一配置。
